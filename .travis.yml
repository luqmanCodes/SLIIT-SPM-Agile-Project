language : node_js
node_js :
  - "6.0"
script :
  - npm test
  - npm run build
install:
  - npm install -g firebase-tools
  - cd front-end
  - npm install
  - cd..
  - cd functions
  - npm install
after_success:
  - firebase deploy --token $FIREBASE_TOKEN


